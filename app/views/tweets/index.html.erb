<section class="section">
  <div class="container">
    <div class="columns">
    
      <% if user_signed_in?%>
        <%= render 'who-to-follow'%>
        <%= render 'feed'%>
        <%=  'profile'%>
      <%else %>
        <%= render 'trends'%>
        <%= render 'feed'%>
      <%end%>

    </div>
  </div>
</section>
